apiVersion: v1
kind: Service
metadata:
  name: {{ include "radar-driver.fullname" . }}
  labels:
    {{- include "radar-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radar-driver.selectorLabels" . | nindent 4 }}
